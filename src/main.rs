mod memory_types_fn_vars;
// use memory_types_fn_vars::one; // :: means path access in rust

fn main() {
    // one();
    
    // structs in rust
    struct example {
        text : String,
        number : i32,
    }


}